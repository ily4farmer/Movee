export default function form() {

    const name = document.querySelector('.form__input[type=text]'),
          tel = document.querySelector('.form__input[type=tel]')
    console.log(tel);
    function formValidate(params) {
        
    }
}